{
  "name": "questionnaire_plomberie",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start-backend\" \"npm run start-frontend\"",
    "start-backend": "cd backend && npm install && npm start",
    "start-frontend": "cd frontend && npm install && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
